/**
 * Amount of margin placed on <body> by default by Material UI. This had to be overridden in index.html so that the appbar would fill the whole upper area without gutters
 */
export const MATERIAL_UI_MARGIN = '8px'
